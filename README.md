# Resilient Web Automation Lab

Overview, setup, and how to run the site + bot + tests.

## Structure
- `sandbox_site/` — local listing site with a configurable chaos engine
- `bot/` — Playwright automation bot that survives the chaos
- `docs/` — planning, scenario coverage matrix, and AI usage log
- `tests/` — unit + scenario tests
- `runs/` — logs + screenshots (gitignored; commit 1–2 samples)

## Setup
_TODO: how to run the site, the bot, a single scenario demo, and the tests._

## Known limitations
_TODO_
